#ifndef HEADER_H
#define HEADER_H
#include <stdint.h>

#define WINDOW_WIDTH (1080)
#define WINDOW_HEIGHT (720)

#define FPS (60)
#define MAX_VELOCITY_Y (20)
extern Uint8 *key_state;
Uint8 *key_state;

#endif